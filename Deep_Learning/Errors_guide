1. import theano 
[error] sd
-> no section 'bias'
[solution]
$ pip uninstall theano 
$ conda install theano

2. from keras.layers import TimeDistributedDense
[error]
-> cannot import name 'TimeDistributedDense' from 'keras.layers' ~ init.py
[solution]
sol 1. do not import TimeDistributedDense
sol 2. downgrade keras to 1.x 
sol 3. define TimeDistributedDenseLayer 

3. imort ConfigParser
[error]
-> ModuleNotFoundError: No module named 'ConfigParser'
[solution]
$ try:
$     import configparser
$ except:
$     from six.moves import configparser

4. theano.sandbox.cuda.use('gpu0')
-> module 'theano.sandbokx' has no attribute 'cuda' 
[solution]  - ?

5. return isinstance(x, tf_ops._TensorLike) or tf_ops.is_dense_tensor_like(x)
[error]
-> AttributeError: module 'tensorflow.python.framework.ops' has no attribute '_TensorLike'
[solution]
-> keras.models import Sequential => tensorflow.keras.models import Sequential 

6. model.add(Dense(512, input_dim=4096,init='glorot_normal',W_regularizer=l2(0.001),activation='relu'))
[error]
-> init related error 
[solution]
-> Dense(10, init="glorot_normal", activation="relu") => Dense(10, activation="relu")

7. model.add(Dense(32, W_regularizer=l2(0.001)))
[error]
-> TypeError: ('Keyword argument not understood:', 'W_regularizer')
[solution]
-> W_regularizer => kernel_regularizer

8. 
